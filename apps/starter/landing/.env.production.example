# Copy to .env.production and fill in values. This file is not copied into the
# image (.dockerignore excludes .env*). landing:deploy forwards every key as a
# Fly build secret; landing:secrets imports the same file as runtime secrets.

VITE_APP_NAME=M5 Starter
VITE_APP_URL=
VITE_SERVER_URL=
