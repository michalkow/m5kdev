---
"create-m5kdev": minor
---

create-m5kdev prompts for optional Backend Modules (experimental choices labeled) and `--yes` scaffolds web + always-on only. Workflows is the first carved module flag; later tickets hang the rest on the same feature-flag model.
