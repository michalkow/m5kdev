---
"@m5kdev/backend": minor
"create-m5kdev": minor
---

Kernel serves an optional baked SPA via `spa.root`. Starter ships Docker/Fly Deploy home and Landing; Node engines are `>=24`. `create-m5kdev update` ignores `fly.toml` and `.env.production`.
