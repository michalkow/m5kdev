---
globs: packages/backend/src/modules/**/*.repository.ts,apps/**/server/src/modules/**/*.repository.ts
alwaysApply: false
---

# Module Repository File Style Guide

Derived from current backend stack patterns.

## Conventions

- Prefer `BaseTableRepository<Orm, Schema, ..., Schema["table"]>` for table-bound modules.
- Use `BaseRepository` for non-table or multi-table repositories.
- Keep repositories data-focused: persistence, query composition, and lightweight mapping only.
- Do not import service classes into repositories.
- Use Drizzle operators (`and`, `eq`, `inArray`, `isNull`, etc.) and query helpers from base classes.
- When running inside `this.orm.transaction`, use `tx` consistently; avoid mixing `this.orm` calls in the same transaction flow.
- Leave orchestration (cross-domain workflows, policy decisions, side effects) to services.

## Query Builder (`this.query`)

Prefer `this.query()` for new repository methods. The builder provides compile-time type narrowing via Zod schemas from the module's `.dto.ts` file.

- Use `.input(schema)` to narrow the input type from a DTO input schema.
- Use `.output(schema)` to narrow the output type from a DTO output schema.
- Use the generic type parameter `this.query<InputType>(name)` when no matching DTO input schema exists.
- Use `.nullable()` on output schemas for methods that may return `null`.
- Use `z.array(schema)` for methods that return lists.

## Error Handling: `throwableQuery` vs `throwableAsync`

- Use `throwableQuery` to wrap **individual Drizzle queries** (selects, inserts, updates, deletes, transactions). It converts thrown database errors into `ServerResultAsync`.
- Do **not** wrap entire method bodies in `throwableAsync`. Keep `throwableQuery` scoped tightly around the database call so error-handling logic (branching on `isErr`, returning `this.error(...)`) stays outside the wrapper.
- The `this.query()` builder already wraps the handler in `throwableAsync` internally, so query-builder methods only need `throwableQuery` around their Drizzle calls.
- Built-in helpers (`queryFind`, `queryList`, `findById`, `create`, …) already use `throwableQuery` internally — do not wrap their return values again.

## Prefer Built-in Base Methods (Strong Default)

`BaseTableRepository` provides typed CRUD helpers. **Always use these before adding custom repository methods or raw Drizzle.** Services should call these helpers directly in most cases.

- `queryList(query, options)` for filtered/sorted/paginated lists.
- `queryFind(query, options)` for filtered/sorted single-row lookup (no pagination/count).
- `findById(id)` / `findManyById(ids)` for primary-key lookups.
- `create(data)` / `createMany(data)` for inserts.
- `update(data)` / `updateMany(data)` for updates.
- `softDeleteById(id)` / `deleteById(id)` for deletes.

### Usually Do Not Add Custom List / Getter / Create / Update Methods

- **Lists and getters**: `queryList` / `queryFind` already expose read APIs extended via `filters` (and optional `conditions`). Prefer calling them from the service with the needed filters. Do not add thin `listPublished`, `findBySlug`, `getMain`, etc. unless the same filter set is reused heavily *and* belongs in the repository boundary.
- **Creates**: `create` is permissive — stamp defaults (`status`, `memberId`, …) in the **service** and pass them to `create`. Do not add a custom repository `create*` just to apply defaults.
- **Updates**: `update` accepts partial payloads — omit fields you do not want to change. Do not add a custom repository `update*` just to restrict columns or merge patches.
- Write raw `this.orm.select()...` or a custom method only for joins, CTEs, multi-table writes, transactions, or predicates that cannot be expressed with filters / `TableConditionBuilder`.

### If a Shared Lookup Belongs in the Repository

Only then wrap built-ins with filters — never reimplement select/where/limit. Prefer the same filter call from the **service** when the lookup is used once:

```typescript
import { BaseTableRepository } from "@m5kdev/backend/modules/base/base.repository";
import { z } from "zod";
import type { Orm, Schema } from "#db";
import { itemSchemas } from "./items.dto";

export class ItemRepository extends BaseTableRepository<
  Orm,
  Schema,
  Record<string, never>,
  Schema["items"]
> {
  // Shared domain lookup used from multiple services/jobs — still no hand-written SQL
  findBySlug = this.query("findBySlug")
    .input(z.object({ slug: z.string() }))
    .output(itemSchemas.output.found)
    .handle(({ slug }) =>
      this.queryFind({
        filters: [
          { columnId: "slug", type: "string", method: "equals", value: slug },
        ],
      })
    );
}
```

Filter shape: `{ columnId, type, method, value }` (`type`: `string` | `number` | `date` | `boolean` | `enum` | `jsonArray`; common `method`s: `equals`, `contains`, `oneOf`, `is_null`, …). Soft-delete exclusion is applied automatically when the table has `deletedAt` (unless `showDeleted: true`).

### Extend With `conditions` When Filters Alone Are Not Enough

Pass a `TableConditionBuilder` via `options.conditions` for extra predicates (optional excludes, `ne`, `inArray`, JSON helpers, etc.) while still reusing sorting, soft-delete, and column picking from `queryFind` / `queryList`:

```typescript
import { ne } from "drizzle-orm";
import { z } from "zod";
import { itemSchemas } from "./items.dto";

// Prefer calling queryFind with conditions from the service when one-off;
// repository wrapper only if shared across callers.
findBySlug = this.query("findBySlug")
  .input(
    z.object({
      slug: z.string(),
      excludeId: z.string().optional(),
    })
  )
  .output(itemSchemas.output.found)
  .handle(({ slug, excludeId }) => {
    const conditions = this.getConditionBuilder(this.table);
    if (excludeId) {
      conditions.push(ne(this.table.id, excludeId));
    }

    return this.queryFind(
      {
        filters: [
          { columnId: "slug", type: "string", method: "equals", value: slug },
        ],
      },
      { conditions }
    );
  });
```

## When to Use `async` Methods Instead of Query Builder

Use regular `async` methods when:

- The method requires an optional `tx` parameter for transaction support.
- The method orchestrates multiple sequential queries with branching logic between them.
- The method wraps a `this.orm.transaction()` call.

## Example: Raw Drizzle (Only When Built-ins Do Not Fit)

```typescript
findWithCategory = this.query("findWithCategory")
  .input(z.object({ id: z.string() }))
  .output(itemSchemas.output.withCategory.nullable())
  .handle(async ({ id }) => {
    const result = await this.throwableQuery(() =>
      this.orm
        .select({
          item: this.schema.items,
          categoryName: this.schema.categories.name,
        })
        .from(this.schema.items)
        .innerJoin(
          this.schema.categories,
          eq(this.schema.items.categoryId, this.schema.categories.id)
        )
        .where(eq(this.schema.items.id, id))
        .limit(1)
    );
    if (result.isErr()) return err(result.error);
    const [row] = result.value;
    return row ?? null;
  });
```

## Example: Async Method (Transaction / Multi-Query)

```typescript
async transferOwnership(
  { fromUserId, toUserId, itemId }: { fromUserId: string; toUserId: string; itemId: string },
  tx?: Orm
): ServerResultAsync<{ status: boolean }> {
  const db = tx ?? this.orm;

  const itemResult = await this.throwableQuery(() =>
    db.select().from(this.schema.items).where(eq(this.schema.items.id, itemId)).limit(1)
  );
  if (itemResult.isErr()) return err(itemResult.error);
  const [item] = itemResult.value;
  if (!item) return this.error("NOT_FOUND");
  if (item.userId !== fromUserId) return this.error("FORBIDDEN");

  const updateResult = await this.throwableQuery(() =>
    db.update(this.schema.items).set({ userId: toUserId }).where(eq(this.schema.items.id, itemId)).returning()
  );
  if (updateResult.isErr()) return err(updateResult.error);
  return ok({ status: true });
}
```
